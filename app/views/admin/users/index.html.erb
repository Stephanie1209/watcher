<table>
  <tr>
    <th>Name</th>
    <th>Link</th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= link_to "Edit", edit_admin_user_path(user) %></td>
    </tr>
  <% end %>
</table>

